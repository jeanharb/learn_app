<%= form_for(current_user.courseregistrations.find_by_takencourse_id(@course.id), 
			 html: { method: :delete }) do |f| %>
  <div><%= f.hidden_field :takencourse_id %></div>
  <%= f.submit "Unregister", :class => "reg-btn reg-btn-rem" %>
<% end %>