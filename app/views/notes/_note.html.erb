<% if !note.filename.nil? %>
<li>
	<span class="content">
		<%= form_for note, :url => {:action => coursefile(note)} do |f| %>
		<%= f.submit "Create" %>
		<% end %>
	</span>
	<span class="timestamp">
		Posted <%= time_ago_in_words(note.created_at) %> ago.
	</span>
</li>
<% end %>